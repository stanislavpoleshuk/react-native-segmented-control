import * as React from 'react';
import SegmentedControl from './components/segmented-control';

export type SegmentedControlRefProps = React.ElementRef<
  typeof SegmentedControl
>;

export default SegmentedControl;
